name: Render Pixlet WebP

on:
  push:
    branches: [ main ]
    paths:
      - 'pixlet/**'
      - '.github/workflows/render-pixlet.yml'
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Download Pixlet
        run: |
          curl -L -o pixlet.tar.gz https://github.com/tronbyt/pixlet/releases/latest/download/pixlet_v0.52.0_linux_amd64.tar.gz
          tar -xzf pixlet.tar.gz
          chmod +x pixlet
          sudo mv pixlet /usr/local/bin/pixlet
          pixlet version

      - name: Render Pixlet app
        run: |
          mkdir -p out
          pixlet render pixlet/fuzzyclock.star -o out/fuzzyclock.webp

      - name: Create index page
        run: |
          cat > out/index.html <<'EOF'
          <!DOCTYPE html>
          <html>
          <head><meta charset="utf-8"><title>Pixlet Outputs</title></head>
          <body>
            <h1>Pixlet Outputs</h1>
            <ul>
              <li><a href="fuzzyclock.webp">fuzzyclock.webp</a></li>
            </ul>
          </body>
          </html>
          EOF

      - name: Upload Pages artifact
        uses: actions/upload-pages-artifact@v3
        with:
          path: out/

  deploy:
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v4
